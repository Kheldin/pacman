[project]
name = "pac-man"
version = "0.1.0"
description = "The good old pac-man"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flake8==7.3.0",
    "mypy==1.20.2",
    "pydantic==2.13.3",
]

[tool.mypy]
explicit_package_bases = true

ignore_missing_imports = true